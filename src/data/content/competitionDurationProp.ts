import { CompetitionDurationProps } from "sections";

export const competitionDurationProps: CompetitionDurationProps = {
  text: "Competitions last 24 hours or when all players hit their match limit"
};
