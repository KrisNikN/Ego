import { RegisterProps } from "collections/PopUps";
import { registerFormProps } from "./registerFormProps";

export const registerPopupProps: RegisterProps = {
  formProps: registerFormProps,
  loginButtonText: "LOGIN",
  title: "Register"
};
